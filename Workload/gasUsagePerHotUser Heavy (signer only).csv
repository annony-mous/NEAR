WITH target_actions AS (
  -- Step 1: Actions initiated by hot users
  SELECT
    signer_account_id AS hot_user,
    converted_into_receipt_id
  FROM `bigquery-public-data.crypto_near_mainnet_us.transaction_actions`
  WHERE signer_account_id IN (
    'relay.tg',
    '0-relay.hot.tg',
    'hotwallet.kaiching',
    'meteor-relayer.near',
    'users.kaiching',
    'here.tg',
    'sweat_welcome.near',
    'spin.sweat',
    '0here.tg',
    'relay.aurora'
  )
  AND block_date BETWEEN '2024-03-31' AND '2024-04-06'
)

SELECT
  hot_user,

  COUNT(eo.receipt_id) AS executed_receipt_count,

  SUM(eo.gas_burnt) / 1e12 AS total_tgas_burnt,
  SAFE_DIVIDE(SUM(eo.gas_burnt), COUNT(eo.receipt_id)) / 1e12 AS avg_tgas_per_action

FROM target_actions ta
JOIN `bigquery-public-data.crypto_near_mainnet_us.execution_outcomes` eo
  ON ta.converted_into_receipt_id = eo.receipt_id

GROUP BY hot_user
ORDER BY total_tgas_burnt DESC;
