WITH deployed_contracts AS (
  SELECT DISTINCT
    receiver_account_id AS contract_id
  FROM `bigquery-public-data.crypto_near_mainnet_us.transaction_actions`
  WHERE action_kind IN (
    'DEPLOY_CONTRACT',
    'USE_GLOBAL_CONTRACT',
    'USE_GLOBAL_CONTRACT_BY_ACCOUNT_ID'
  )
)

SELECT
  CASE
    WHEN ta.receiver_account_id < '650' THEN 10
    WHEN ta.receiver_account_id < 'aurora' THEN 11
    WHEN ta.receiver_account_id < 'aurora-0' THEN 1
    WHEN ta.receiver_account_id < 'earn.kaiching' THEN 8
    WHEN ta.receiver_account_id < 'game.hot.tg' THEN 9
    WHEN ta.receiver_account_id < 'game.hot.tg-0' THEN 6
    WHEN ta.receiver_account_id < 'kkuuue2akv_1630967379.near' THEN 7
    WHEN ta.receiver_account_id < 'tge-lockup.sweat' THEN 4
    ELSE 5
  END AS shard_id,

  -- distinct deployed contracts used
  COUNT(DISTINCT ta.receiver_account_id) AS distinct_contract_count,

  -- total usage count
  COUNT(*) AS usage_count

FROM `bigquery-public-data.crypto_near_mainnet_us.transaction_actions` ta
WHERE ta.action_kind IN (
      'FUNCTION_CALL',
      'USE_GLOBAL_CONTRACT',
      'USE_GLOBAL_CONTRACT_BY_ACCOUNT_ID'
    )
  AND ta.receiver_account_id IS NOT NULL
  AND ta.receiver_account_id IN (
    SELECT contract_id FROM deployed_contracts
  )
  AND ta.block_date >= '2025-08-18'
  AND ta.block_date <= '2025-12-31'

GROUP BY shard_id
ORDER BY shard_id;
