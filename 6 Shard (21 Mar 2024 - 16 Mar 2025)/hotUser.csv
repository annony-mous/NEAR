-- Step 1: Identify all contract accounts
WITH contract_accounts AS (
  SELECT DISTINCT receiver_account_id AS contract_id
  FROM `bigquery-public-data.crypto_near_mainnet_us.transaction_actions`
  WHERE action_kind IN ('DEPLOY_CONTRACT', 'FUNCTION_CALL', 'USE_GLOBAL_CONTRACT', 'USE_GLOBAL_CONTRACT_BY_ACCOUNT_ID')
    AND receiver_account_id IS NOT NULL
),

-- Step 2: Collect all user-related actions (signers or receivers) excluding contracts as users
user_actions AS (
  SELECT signer_account_id AS account_id
  FROM `bigquery-public-data.crypto_near_mainnet_us.transaction_actions`
  WHERE signer_account_id NOT IN (SELECT contract_id FROM contract_accounts)
    AND signer_account_id IS NOT NULL
    AND block_date > '2024-03-21' AND block_date <= '2025-03-16'

  UNION ALL

  SELECT receiver_account_id AS account_id
  FROM `bigquery-public-data.crypto_near_mainnet_us.transaction_actions`
  WHERE receiver_account_id NOT IN (SELECT contract_id FROM contract_accounts)
    AND receiver_account_id IS NOT NULL
    AND block_date > '2024-03-21' AND block_date <= '2025-03-16'
),

-- Step 3: Count total activity per account
user_total_activity AS (
  SELECT
    account_id,
    COUNT(*) AS total_actions
  FROM user_actions
  GROUP BY account_id
)

-- Step 4: Top 100 most active accounts
SELECT *
FROM user_total_activity
ORDER BY total_actions DESC
LIMIT 100;
